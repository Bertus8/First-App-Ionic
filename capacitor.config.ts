import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter', // domain name & app identifier tecknyks.com firstApp
  appName: 'first-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
